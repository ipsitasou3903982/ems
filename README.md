Employee Management System

Steps To Run an Employee Management System

Get the ems folder from the zip.
Paste it in the XAMPP inside the htdocs folder.
Open the folder in VS Code.
Open the terminal ( shortcut CTRL + ` )
Run - php spark serve
Run - php spark migrate
Run - php spark db:seed DatabaseSeeder
